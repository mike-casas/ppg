
text = "<%= j render 'link_filename' %>"
$('#list_file').append(text);

obj={
   evento: "event_file_create",
   data: text
}

 msg = JSON.stringify(obj);

room.emit('event_file_create', msg);

$('#file_name').val('');
