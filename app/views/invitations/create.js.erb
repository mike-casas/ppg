<%  if @project.errors.any? %>


alert('<%= @project.errors.full_messages[0] %>');

<% else%>

  text= "<%= j render 'projects/tip_member'%>"

  $('#list-member').append(text);

obj={
 evento: "invitations",
 text: text,
 data_name:  "<%= @project.name %>",
 data_id:  "<%= @project.id %>",
 room: "<%= @guest.id %>",
 guest: "<%= @guest.name %>",
 owner_img: "<%= @owner_img %>"
}

msg = JSON.stringify(obj);


room.emit('invitations', msg);




<%end %>

